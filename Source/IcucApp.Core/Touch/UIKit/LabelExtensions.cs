namespace IcucApp.Core.Touch.UIKit
{
    public static class LabelExtensions
    {
        //public static UILabel AlignTop(this UILabel label)
        //{
        //    SizeF fontSize = label.StringSize(label.Text, label.Font);
        //    float finalHeight = fontSize.Height * label.Lines;
        //    float finalWidth = label.Frame.Size.Width;
        //    SizeF theStringSize = label.StringSize(label.Text, label.Font, new SizeF(finalWidth, finalHeight), UILineBreakMode.Clip);
        //    var newLinesToPad = (int)((finalHeight - theStringSize.Height) / fontSize.Height);
        //    for (int i = 0; i <= newLinesToPad; i++)
        //    {
        //        label.Text = label.Text + " \n";
        //    }
        //    return label;
        //}
    }
}